# Backend

Use `server.js`.

The application runtime is the Node 24 server with built-in SQLite:

```powershell
node backend/server.js
```

`server.py`, if present in a OneDrive workspace, is a stale ignored placeholder and is not used by the app.
